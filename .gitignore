# Node / Next
node_modules/
.next/

# Environment files
.env
.env.*
!.env.example
!.env*.example

# Wenlan web local env
apps/web/.env.local
apps/web/.env.production.local
apps/web/.env.development.local

# Self-hosted Supabase runtime secrets
infra/supabase/runtime/*
!infra/supabase/runtime/.gitkeep

# Build artifacts
dist/
coverage/
*.tar
*.tar.gz
*.zip

# Runtime logs and process files
*.log
*.err.log
*.out.log
*.pid
apps/web/.next-logs/
apps/web/.run-logs/
apps/web/.migration-backups/
.claude/

# Local secret inventories / handoff notes
**/SECRET-INVENTORY.md
**/SECRET-BACKUP.md

# Local archives and deployment bundles
wenlan-web-deploy*.tar.gz

# 根目录临时产物（飞书迁移脚本 / 测试截图 / 临时数据，可能含 token 与用户数据，禁止入库）
/*.json
/*.ps1
/*.png
/*.xml
/.codex-logs/
/apps/web/.codex-logs/

